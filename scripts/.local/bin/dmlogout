#!/bin/sh

action=$(printf "lock\nlogout\nreboot\nshutdown" | dmenu -p "System Exit Action?: ")

case $action in
  shutdown)
    sudo poweroff
    ;;
  reboot)
    sudo reboot
    ;;
  logout)
    pkill -KILL -u $USER 
    ;;
  lock)
    slock
    ;;
  *)
    ;;
esac
